// No-op placeholder. The playwright config references this path; the
// real shared-test-user setup lives in individual specs via api-mocks.
export default async function globalSetup() {
  // intentionally empty
}
